# Logging for 2026-07-12

> Token counts and CO2 use the conventions in [`../../logging.md`](../../logging.md)
> (0.076 gCO2 per 1,000 tokens). Authoritative per-prompt numbers are in
> `Logs/log_summary.csv` (written by the `Stop` hook). NOTE: the hook excludes
> sidechain (subagent) turns, so work delegated to Fable subagents is NOT captured
> in the CSV — its tokens are noted inline per entry.

---

## Entry 1

- **UT time:** 2026-07-12T13:48:39Z (prompt received)
- **Prompt:** "Re-read context_prompts.md in Projects/night-planner/claude_prompts/. Execute the 1st prompt in the Websites/Night plans section. Use Fable if you can"
- **Model:** main session claude-opus-4-8; **substantive work ran on the Fable 5
  model (`claude-fable-5`) via a delegated subagent** ("Use Fable if you can").
  Stop hook excludes sidechain turns, so the Fable subagent's ~79.2k tokens are
  NOT in `log_summary.csv`; the CSV row is main-session only.
- **Tokens/CO2:** see `log_summary.csv` (main-session only; add ~79.2k Fable).

**Work done / thinking:**
- Executed the "Night plans" prompt: read `/mnt/scratch/xavier/Observing` (note:
  the prompt's path was updated from the old `context/Night_plans/`) and summarize
  what it teaches about how the astronomers plan a night, into `claudes_context.md`.
- **Scouted the archive from the main session first** (4433 files, mostly binary),
  then handed a directory map + a sampling strategy to a single Fable 5 subagent
  (kept to one agent so the shared context-file write stayed atomic). Told it to
  read representative human-readable planning files rather than all 4433, and to
  extract `.docx` via `unzip -p ... word/document.xml`.
- **Two trees:** `Observing/<Instrument>/<Run>/` = the planning material (gold);
  `Observations/<Facility>/` = reduced data (AAO, ALMA, Gemini-N/S, HST, JWST,
  Keck, Lick_Kast, Magellan, MeerTRAP_HighDM, MMT, NOT, Pepsi, SOAR, VLT).
- **Key learnings captured** (now in context + brain):
  - **Run-folder convention** — each run = night-plan `.docx` + Keck starlist
    `.txt` + `inst_config.txt` (SIAS dump) + target `.xlsx`/`Targets.docx` +
    finder charts + MOS mask files (`.lst`/`.obj`/DS9 `.reg`) + `Obs_logs*.xlsx`
    + backup docs. This folder is itself a template night-planner should emulate.
  - **Keck starlist format** (verified across DEIMOS/HIRES/LRIS/KCWI):
    `name HH MM SS.ss ±DD MM SS.s equinox [keyword=value …]`; observed keywords
    `rotdest`, `rotmode=PA`, `raoffset`/`decoffset`, `vmag`, `pmra`/`pmdec`, `lgs`;
    offset stars are `_OFF` companion entries sharing the target's `rotdest`.
  - **Night-plan doc anatomy** — links (incl. ucolick ephemeris) → ranked
    priorities → per-program setup tables → twilight block (focus/standard/align)
    → **LST-keyed timeline** bounded by the 18°-twilight LST range with per-block
    exposures/contingencies + mid-night MIRA → calibration checklist → Backup
    Plans → SA contact → troubleshooting diary → pre-run TODO.
  - **ToO** — compact single-target recipe (trigger/goals/setup/target+offset-star
    acquisition rules), no LST timeline.
- **Deliverables verified:** `claudes_context.md` gained "## Observing archive: how
  the astronomers plan a night" (line 492, 7 subsections); brain gained a "The
  astronomers' real night-planning practice" subsection + v0.8 entry. All `.docx`
  extracted cleanly; binaries deliberately skimmed at directory level only. Ran no
  git commands.
---
